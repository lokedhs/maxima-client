name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Load the application
      run: |
        sbcl --eval '(ql:quickload "infoparser")'
